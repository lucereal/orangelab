# Snippet: require passwords and load the token-dock ACL.
# Merge into your existing mosquitto.conf (or HA Mosquitto add-on config).

allow_anonymous false
password_file /mosquitto/config/passwd
acl_file /mosquitto/config/acl
